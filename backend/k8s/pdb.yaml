apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: chioma-backend-pdb
  namespace: production
  labels:
    app: chioma-backend
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: chioma-backend
  unhealthyPodEvictionPolicy: AlwaysAllow
